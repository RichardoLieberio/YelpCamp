<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/custom.css">
  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.min.js"></script>
  <title>Login YelpCamp</title>
</head>
<body>
  <% if(message) { %>
    <div class="alert alert-success position-absolute top-0 start-0 w-100" role="alert"><%= message %></div>
  <% } %>
  <div class="w-100 vh-100 d-flex justify-content-center align-items-center">
    <form action="login" method="post" style="min-width: 80px" class="container w-25" autocomplete="off">
      <h1 class="text-center mb-4">Login Form</h1>

      <div data-mdb-input-init class="form-outline mb-4">
        <label class="form-label" for="email">Email address</label>
        <input type="email" id="email" name="email" class="form-control"/>
      </div>

      <div data-mdb-input-init class="form-outline mb-4">
        <label class="form-label" for="pwd">Password</label>
        <input type="password" id="pwd" name="pwd" class="form-control"/>
        <% if(invalid) { %>
          <small class="form-text text-danger"><%= invalid %></small>
        <% } %>
      </div>

      <div class="row mb-4">
        <div class="col d-flex justify-content-center">
          <div class="form-check">
            <input class="form-check-input" type="checkbox" value="true" id="remember_me" name="remember_me"/>
            <label class="form-check-label" for="remember_me">Remember me</label>
          </div>
        </div>
        <div class="col">
          <a href="/forgot-password">Forgot password?</a>
        </div>
      </div>

      <button data-mdb-ripple-init type="submit" class="btn btn-primary btn-block w-100 mb-4">Login</button>

      <p class="text-center">Don't have an account yet? <a href="register">Register</a></p>
    </form>
  </div>
</body>
</html>